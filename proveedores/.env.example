DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Portal (punto de entrada único) — botón "Volver al Portal" en el menú.
# Opcional: si no se define, usa el Portal de Sumivensa ya desplegado.
NEXT_PUBLIC_PORTAL_URL="https://portal-sumivensa-client.vercel.app"
