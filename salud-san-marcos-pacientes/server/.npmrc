# Permite que los scripts de instalación de las dependencias (ej. esbuild, usado
# internamente por tsx) corran normalmente. Sin esto, versiones recientes de npm
# bloquean esos scripts por defecto y la instalación puede fallar o quedar incompleta.
dangerously-allow-all-scripts=true
